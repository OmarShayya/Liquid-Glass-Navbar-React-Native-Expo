/**
 * expo-liquid-glass-tabs/router — Expo-Router-only entry.
 *
 * These components import `expo-router`, so they live in a separate entry. Apps that don't use
 * Expo Router should NOT import from here (and don't need expo-router installed).
 *
 *   import { NativeLiquidGlassTabBar, LiquidGlassTabs } from 'expo-liquid-glass-tabs/router';
 */
export { LiquidGlassTabs } from './LiquidGlassTabs';
export { NativeLiquidGlassTabBar } from './NativeLiquidGlassTabBar';
